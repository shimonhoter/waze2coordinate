<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Waze2Coordinate"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">

            <!-- פתיחה רגילה מהלאנצ'ר -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- קבלת טקסט משותף (Share -> Waze2Coordinate) מכל אפליקציה, כולל Waze עצמו -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- פתיחה ישירה כשמשתמש לוחץ על קישור waze.com/ul/... ובוחר את האפליקציה הזו -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="waze.com" android:pathPrefix="/ul" />
                <data android:scheme="https" android:host="www.waze.com" android:pathPrefix="/ul" />
            </intent-filter>

            <!-- פתיחה ישירה מקישור Google Maps מקוצר (maps.app.goo.gl, goo.gl/maps) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="maps.app.goo.gl" />
                <data android:scheme="https" android:host="goo.gl" android:pathPrefix="/maps" />
            </intent-filter>

        </activity>

        <!-- FileProvider - נדרש לשיתוף תמונת snapshot של המפה עם אפליקציות חוץ (WhatsApp וכו'),
             כי מ-Android 10 ואילך לא ניתן לשתף קבצים ישירות עם file:// URI בין אפליקציות -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
